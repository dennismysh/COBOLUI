[package]
name = "cobalt-ir"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Intermediate representation types for the COBALT UI framework"

[dependencies]
serde = { version = "1", features = ["derive"] }
